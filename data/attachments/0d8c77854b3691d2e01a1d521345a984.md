# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: specs/smoke-testing/09-calendar-scheduling/sensei.teaching-filter.spec.ts >> SEN-011 - the staff teaching filter >> as a sensei >> SEN-011 - teaching mode shows a class the sensei teaches, by a name-derived filter @SEN-011 @destructive
- Location: e2e/specs/smoke-testing/09-calendar-scheduling/sensei.teaching-filter.spec.ts:177:9

# Error details

```
Error: switching to Teaching mode with a real class on today's calendar should show the "Teaching · N events" count - it only renders once viewEventCount > 0 (ClubCalendar.tsx line 1087).

expect(locator).toBeVisible() failed

Locator: getByText(/^(Teaching|Attending)\s*·\s*\d+\s*events?$/i).filter({ visible: true }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - switching to Teaching mode with a real class on today's calendar should show the "Teaching · N events" count - it only renders once viewEventCount > 0 (ClubCalendar.tsx line 1087). with timeout 15000ms
  - waiting for getByText(/^(Teaching|Attending)\s*·\s*\d+\s*events?$/i).filter({ visible: true }).first()

```

```yaml
- complementary:
  - button "Expand sidebar"
  - navigation:
    - link "Home":
      - /url: /dashboard
    - link "Calendar":
      - /url: /dashboard/myClub/cms4gce000003pkwujx5570uv/calendar
    - link "Message":
      - /url: /dashboard/messages
    - link "Progress":
      - /url: /progress
    - link "Library":
      - /url: /dashboard/myClub/cms4gce000003pkwujx5570uv/library?standalone=true
    - link "Support":
      - /url: /support
    - link "Curriculum":
      - /url: /dashboard/myClub/cms4gce000003pkwujx5570uv/curricula
    - link "My Payments":
      - /url: /dashboard/myClub/cms4gce000003pkwujx5570uv/user-payments
    - link "My Club":
      - /url: /dashboard/myClub
    - button "Share"
- banner:
  - link "Martial Apps Logo Martial Apps · Automation Club":
    - /url: /dashboard
    - img "Martial Apps Logo"
    - text: Martial Apps · Automation Club
  - button "Send feedback"
  - link "76":
    - /url: /dashboard/messages
  - button "51"
  - button "S"
  - button "Overview"
  - button "Sites"
- main:
  - button "Collapse sidebar"
  - button "Create"
  - button "September 2026"
  - button
  - button
  - text: S M T W T F S 30 31
  - button "1"
  - button "2"
  - button "3"
  - button "4"
  - button "5"
  - button "6"
  - button "7"
  - button "8"
  - button "9"
  - button "10"
  - button "11"
  - button "12"
  - button "13"
  - button "14"
  - button "15"
  - button "16"
  - button "17"
  - button "18"
  - button "19"
  - button "20"
  - button "21"
  - button "22"
  - button "23"
  - button "24"
  - button "25"
  - button "26"
  - button "27"
  - button "28"
  - button "29"
  - button "30"
  - text: 1 2 3
  - button "Curricula":
    - heading "Curricula" [level=3]
  - button "View all →"
  - text: "[e2e] Automation [e2e] OWP-017 Curriculum [e2e] SEN-006 Curriculum [e2e] SEN-011 Target Curriculum"
  - button "Sites":
    - heading "Sites" [level=3]
  - button "View all →"
  - button "All Sites"
  - button "Automation Club - Main Site"
  - button "[e2e] Canada TZ - AB (Mountain)"
  - button "[e2e] Canada TZ - BC (Pacific)"
  - button "[e2e] Canada TZ - MB (Central)"
  - button "[e2e] Canada TZ - NL (Newfoundland)"
  - button "[e2e] Canada TZ - NS (Atlantic)"
  - button "[e2e] Canada TZ - ON (Eastern)"
  - button "[e2e] Exam Fixtures Site"
  - button "[e2e] site-sen011 mto72i0p"
  - button "Event Types":
    - heading "Event Types" [level=3]
  - button "Create"
  - text: Classes Exams Workshops Socials Tournaments
  - button "Expand sidebar"
  - button "Today"
  - button
  - button
  - heading "Saturday, September 5, 2026" [level=2]
  - button "Sensei alnf"
  - button "Teaching"
  - button "Attending"
  - button "Day"
  - button "Week"
  - button "Month"
  - button "Bulk assign"
  - text: 6 AM 7 AM 8 AM 9 AM 10 AM 11 AM 12 PM 1 PM 2 PM 3 PM 4 PM 5 PM 6 PM 7 PM 8 PM 9 PM 10 PM
  - paragraph: No events scheduled
  - paragraph: Click a time slot to get started
  - button
- alert
```

# Test source

```ts
  133 |       });
  134 | 
  135 |       await clubSection('calendar').gotoAndAwaitClubRole(clubRoute(clubId, 'calendar'));
  136 | 
  137 |       await expect(
  138 |         calendarLocators.attendingToggle(page),
  139 |         'a sensei is staff, so the calendar should offer the Attending side of ' +
  140 |           'the filter.',
  141 |       ).toBeVisible({ timeout: 15_000 });
  142 | 
  143 |       await expect(
  144 |         calendarLocators.teachingToggle(page),
  145 |         'and the Teaching side.',
  146 |       ).toBeVisible({ timeout: 15_000 });
  147 | 
  148 |       /**
  149 |        * Step 2 - both sides accept a click. The resulting MODE is not asserted
  150 |        * here: the only thing that reports it is the "Attending · N events"
  151 |        * label, which `ClubCalendar.tsx` line 1089 renders solely when
  152 |        * `viewEventCount > 0`. On a calendar with no events for this sensei
  153 |        * there is nothing to read, so claiming the mode changed would be
  154 |        * claiming more than the screen shows.
  155 |        */
  156 |       await calendarLocators.teachingToggle(page).click();
  157 |       await calendarLocators.attendingToggle(page).click();
  158 |     });
  159 | 
  160 |     /**
  161 |      * Steps 1 and 3, which the pair above could not reach on an empty
  162 |      * calendar - both `viewEventCount > 0` (the "N events" label) and the
  163 |      * teacher dropdown's own presence depend on the sensei actually having
  164 |      * something on screen. `@destructive`: builds one real class for TODAY
  165 |      * (see `calendar.page.ts`'s header on why - `openTodayEvent` needs the
  166 |      * day view's own default date, and the calendar's navigation could not
  167 |      * be driven).
  168 |      *
  169 |      * The class is left with NO teacher explicitly assigned, on purpose -
  170 |      * `ClubCalendar.tsx`'s teaching-mode filter (line 274-285) shows an
  171 |      * event under `mine` if `teacherMemberIds` includes the viewer OR if
  172 |      * NOBODY is assigned yet ("so they can be claimed"). An unassigned class
  173 |      * already proves the "mine" default surfaces it, without this test also
  174 |      * having to drive the popup's separate teacher-reassignment picker
  175 |      * (`SEN-003` already covers that control on its own terms).
  176 |      */
  177 |     test('SEN-011 - teaching mode shows a class the sensei teaches, by a name-derived filter @SEN-011 @destructive', async ({
  178 |       page,
  179 |       sitesPage,
  180 |       clubSection,
  181 |       clubId,
  182 |       ownerApi,
  183 |     }, testInfo) => {
  184 |       testInfo.annotations.push({
  185 |         type: 'manual-scenario',
  186 |         description: 'manual-qa/role-sensei.md#SEN-011 (steps 1, 3)',
  187 |       });
  188 | 
  189 |       await ensureAutomationCurriculum(ownerApi, clubId);
  190 | 
  191 |       const sensei = await findMemberIdByEmail(ownerApi, clubId, ROLES.sensei.email);
  192 |       expect(sensei?.name, `${ROLES.sensei.email} was not found on the roster.`).toBeTruthy();
  193 |       const senseiName = sensei!.name;
  194 | 
  195 |       const className = scratchName('sen011-class');
  196 |       let siteId: string | undefined;
  197 |       let classId: string | undefined;
  198 | 
  199 |       try {
  200 |         // Setup as the OWNER - same reasoning as SEN-003: a setup failure
  201 |         // should never masquerade as a permission failure.
  202 |         siteId = await createScratchSite(ownerApi, clubId, scratchName('site-sen011'));
  203 | 
  204 |         const roomRes = await ownerApi.post(
  205 |           `${env.apiURL}/clubs/${clubId}/sites/${siteId}/rooms`,
  206 |           {
  207 |             headers: { 'x-app-id': env.appId },
  208 |             data: { name: scratchName('room'), capacity: 10, roomsize: '30', floor: '1' },
  209 |           },
  210 |         );
  211 |         const roomBody = await roomRes.json().catch(() => null);
  212 |         const roomId: string | undefined = roomBody?.data?.id ?? roomBody?.data?.roomId;
  213 | 
  214 |         // From here on it is the SENSEI's own session.
  215 |         await sitesPage.openCreateClassForm(
  216 |           fillRoute('room', { clubId, siteId, roomId: roomId as string }),
  217 |           todayIsoUtc(),
  218 |           '10:00',
  219 |         );
  220 |         classId = await sitesPage.createClass(className, AUTOMATION_CURRICULUM_NAME);
  221 | 
  222 |         await clubSection('calendar').gotoAndAwaitClubRole(clubRoute(clubId, 'calendar'));
  223 | 
  224 |         // Step 1 (the part the read-only pair above could not reach) - once
  225 |         // there is a real event today, switching to Teaching mode surfaces
  226 |         // the "Teaching · N events" label.
  227 |         await calendarLocators.teachingToggle(page).click();
  228 |         await expect(
  229 |           calendarLocators.teachingModeLabel(page),
  230 |           'switching to Teaching mode with a real class on today\'s calendar ' +
  231 |             'should show the "Teaching · N events" count - it only renders ' +
  232 |             'once viewEventCount > 0 (ClubCalendar.tsx line 1087).',
> 233 |         ).toBeVisible({ timeout: 15_000 });
      |           ^ Error: switching to Teaching mode with a real class on today's calendar should show the "Teaching · N events" count - it only renders once viewEventCount > 0 (ClubCalendar.tsx line 1087).
  234 | 
  235 |         // Step 3 - the trigger reads the sensei's own name, not
  236 |         // "All teachers". The generic `teacherFilterTrigger` above still
  237 |         // matches an OWNER's "All teachers" default; this one is named.
  238 |         // See `calendar.locators.ts` for why this is the FULL name, not
  239 |         // just the first word - the two renderings of this control disagree
  240 |         // and only one of them is reachable at this suite's viewport.
  241 |         const trigger = calendarLocators.teacherFilterTriggerNamed(page, senseiName);
  242 |         await expect(
  243 |           trigger,
  244 |           `the teacher-filter trigger should read the sensei's own name ` +
  245 |             `("${senseiName}") once the filter defaults to "mine" - not ` +
  246 |             `"All teachers", which is the owner's default only.`,
  247 |         ).toBeVisible({ timeout: 15_000 });
  248 | 
  249 |         await trigger.click();
  250 |         const options = calendarLocators.teacherFilterOptions(trigger);
  251 |         await expect(
  252 |           options.filter({ hasText: senseiName }).first(),
  253 |           'the open dropdown should offer the sensei\'s own full name as one ' +
  254 |             'of its options.',
  255 |         ).toBeVisible({ timeout: 10_000 });
  256 |         await expect(
  257 |           options.filter({ hasText: /^All teachers$/i }).first(),
  258 |           'and an "All teachers" option, to widen the filter back out.',
  259 |         ).toBeVisible({ timeout: 10_000 });
  260 |       } finally {
  261 |         if (classId) {
  262 |           await deleteScratchClass(ownerApi, clubId, classId);
  263 |         }
  264 |         if (siteId) {
  265 |           await deleteScratchSite(ownerApi, clubId, siteId);
  266 |         }
  267 |       }
  268 |     });
  269 | 
  270 |     /**
  271 |      * `SEN-011` step 4 - "Enroll or unenroll a member in a curriculum tied
  272 |      * to a class, then return to the Calendar - it refreshes without a
  273 |      * manual reload."
  274 |      *
  275 |      * ============================================================================
  276 |      * THE RIGHT DOOR WAS THE STAFF "ADD STUDENTS" MODAL, NOT THE PLAIN
  277 |      * SELF-ENROL BUTTON - found by the user, confirmed against the code.
  278 |      * ============================================================================
  279 |      *
  280 |      * The earlier conclusion here ("a sensei has no way to enrol at all")
  281 |      * was checked against only ONE of the app's two enrollment doors. The
  282 |      * plain "Enroll" button on the curricula LIST page is genuinely gated
  283 |      * `canEnroll={!canManage}` (`curricula/page.tsx` line 615) and hidden
  284 |      * for a sensei - that part was right. But a MANAGED curriculum's own
  285 |      * detail view carries a SEPARATE "Add" control (line 1620,
  286 |      * `UserPlus` icon) that opens an "Add Students" panel where staff pick
  287 |      * ANY member - including themselves - and add them via
  288 |      * `POST .../class-definitions/:id/assign-members`
  289 |      * (`curriculumAssignmentRoutes.js` line 754), which explicitly allows
  290 |      * `sensei`. That endpoint's frontend mutation
  291 |      * (`bulkAssignMembersCurriculum`, `classApi.ts` line 450) invalidates
  292 |      * the `'Club'` tag - the SAME tag the calendar's `useGetMyCurriculaQuery`
  293 |      * reads - so a sensei adding THEMSELVES through this modal is a real,
  294 |      * same-session mutation the calendar's cache will react to.
  295 |      *
  296 |      * **Why the sensei has to be the one enrolled, not a different
  297 |      * member.** The calendar's curriculum filter only ever reads the
  298 |      * VIEWER's OWN enrolled curricula (`enrolledCurriculumIds`, sourced
  299 |      * from `useGetMyCurriculaQuery` - a "my own" endpoint, not a lookup on
  300 |      * anyone else). Enrolling a different member changes nothing on the
  301 |      * sensei's own calendar - confirmed by reading the filter, not
  302 |      * assumed. So "enrol a member" here means the sensei adds their OWN
  303 |      * name from that staff modal, which is a real UI action a staff role
  304 |      * can take and a real member ending up enrolled - it just is not the
  305 |      * plain self-service button.
  306 |      *
  307 |      * **Why a SEPARATE fixture curriculum, enrolled into BEFORE this test
  308 |      * ever loads the calendar.** The curriculum filter only activates once
  309 |      * `enrolledCurriculumIds.length > 0` - with zero enrollments, every
  310 |      * event shows regardless, and there would be no visible "before" state
  311 |      * to contrast against "after". So the sensei is enrolled in
  312 |      * `AUTOMATION_CURRICULUM_NAME` via a plain API call FIRST (before the
  313 |      * browser ever loads anything, so there is no session cache to
  314 |      * disturb) - that turns filtering ON. The scratch class is tied to the
  315 |      * SEPARATE `SEN_011_TARGET_CURRICULUM`, which the sensei is not yet
  316 |      * enrolled in, so it starts HIDDEN under the now-active filter. Only
  317 |      * the enrolment done THROUGH THE UI, live, proves the no-reload claim.
  318 |      */
  319 |     test('SEN-011 - enrolling via the staff Add Students modal updates the calendar without a reload @SEN-011 @destructive', async ({
  320 |       page,
  321 |       sitesPage,
  322 |       clubSection,
  323 |       clubId,
  324 |       ownerApi,
  325 |     }, testInfo) => {
  326 |       testInfo.annotations.push({
  327 |         type: 'manual-scenario',
  328 |         description: 'manual-qa/role-sensei.md#SEN-011 (step 4)',
  329 |       });
  330 | 
  331 |       const sensei = await findMemberIdByEmail(ownerApi, clubId, ROLES.sensei.email);
  332 |       expect(sensei?.memberId, `${ROLES.sensei.email} was not found on the roster.`).toBeTruthy();
  333 |       const senseiMemberId = sensei!.memberId;
```