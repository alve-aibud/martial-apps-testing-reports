# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: specs/smoke-testing/11-exams-grading-belts/owner.delete-curriculum.spec.ts >> Owner deletes a curriculum >> OWP-032 - an empty curriculum can be deleted for real @OWP-032 @destructive
- Location: e2e/specs/smoke-testing/11-exams-grading-belts/owner.delete-curriculum.spec.ts:69:7

# Error details

```
Error: clicking Delete should open the "Delete curriculum?" confirmation dialog.

expect(locator).toBeVisible() failed

Locator: getByText(/^(Delete curriculum\?|Supprimer le programme\s*\?)$/i).filter({ visible: true }).first()
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - clicking Delete should open the "Delete curriculum?" confirmation dialog. with timeout 10000ms
  - waiting for getByText(/^(Delete curriculum\?|Supprimer le programme\s*\?)$/i).filter({ visible: true }).first()

```

```yaml
- complementary:
  - button "Expand sidebar"
  - navigation:
    - link "Home":
      - /url: /dashboard
    - link "Calendar":
      - /url: /dashboard/myClub/cmtqw2tih005nmp7kgabxyl0y/calendar
    - link "Message":
      - /url: /dashboard/messages
    - link "Members":
      - /url: /dashboard/myClub/cmtqw2tih005nmp7kgabxyl0y/members
    - link "Progress":
      - /url: /progress
    - link "Library":
      - /url: /dashboard/myClub/cmtqw2tih005nmp7kgabxyl0y/library?standalone=true
    - link "Support":
      - /url: /support
    - link "Payment":
      - /url: /dashboard/myClub/cmtqw2tih005nmp7kgabxyl0y/payments
    - link "My Club":
      - /url: /dashboard/myClub
    - button "Share"
- banner:
  - link "Martial Apps Logo Martial Apps · Automation Club":
    - /url: /dashboard
    - img "Martial Apps Logo"
    - text: Martial Apps · Automation Club
  - button "Send feedback"
  - link "Messages":
    - /url: /dashboard/messages
  - button "16"
  - button "A"
  - button "Overview"
  - button "Members"
  - button "Sites"
  - button "Settings"
- main:
  - button "Go back"
  - heading "Curricula" [level=1]
  - paragraph: Manage class curricula for your club
  - button "Eligible Students"
  - button "Create"
  - main:
    - textbox "Search curricula..."
    - heading "[e2e] owp032-empty-curriculum mtrhrk5f" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] owp032-empty-curriculum mtrhr7pi" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] owp032-empty-curriculum mtrhquw9" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] owp032-empty-curriculum mtrcktik" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] owp032-empty-curriculum mtrckhqy" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] owp032-empty-curriculum mtrck649" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] owp032-empty-curriculum mtr1pqjl" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] SEN-006 Curriculum" [level=3]
    - text: Active
    - button
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] OWP-017 Curriculum" [level=3]
    - text: Active
    - button
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] owp032-empty-curriculum mtr1pab8" [level=3]
    - text: Active
    - button
    - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] SEN-011 Target Curriculum" [level=3]
    - text: Active
    - button
    - paragraph: Permanent fixture for SEN-011 step 4 (the staff Add Students / no-reload calendar check). Safe to leave in place; cannot be deleted through the app.
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - heading "[e2e] Automation" [level=3]
    - text: Active
    - button
    - paragraph: Permanent fixture for the automated write tests. Safe to leave in place; it cannot be deleted through the app (no DELETE route, and Archive is a no-op).
    - text: 0 Members 0 Classes 0% Attendance 0 students enrolled
    - button "Create New"
  - heading "Edit" [level=2]
  - button
  - text: Curriculum Name *
  - textbox "e.g., Young Warriors, Self-Defense Course": "[e2e] owp032-empty-curriculum mtrhrk5f"
  - text: Description (Optional)
  - button "Bold"
  - button "Italic"
  - button "Underline"
  - button "Heading"
  - button "Bullet List"
  - button "Numbered List"
  - button "Undo"
  - button "Redo"
  - paragraph: OWP-032 scratch curriculum - never used, safe to genuinely delete.
  - paragraph: 66/2000
  - text: Min Belt (Optional)
  - combobox: Select
  - text: Max Belt (Optional)
  - combobox: Select
  - text: Age Range (Optional)
  - spinbutton "Min"
  - text: to
  - spinbutton "Max"
  - paragraph: Used to filter eligible students for this curriculum
  - text: Calendar Color
  - button
  - button
  - button
  - button
  - button
  - button
  - button
  - button
  - button
  - button
  - textbox "+": "#6b7280"
  - text: +
  - textbox "#6B7280"
  - button "Cancel"
  - button "Update"
- alert
```

# Test source

```ts
  10  |  * Manual scenario: manual-qa/role-owner-primary.md
  11  |  *   OWP-032  Delete an empty curriculum, and confirm one in use is refused
  12  |  *
  13  |  * ============================================================================
  14  |  * A NEW FEATURE, added 2026-08-21 - a curriculum can be deleted for real now,
  15  |  * but the rule is narrow. Read this before touching curriculum fixtures
  16  |  * anywhere else in this suite.
  17  |  * ============================================================================
  18  |  *
  19  |  * `DELETE /clubs/:clubId/class-definitions/:classDefinitionId`
  20  |  * (`classRoutes.js` line 133, `classService.deleteClassDefinition`) is a
  21  |  * genuine hard delete (`prisma.classDefinition.delete`) - but it first counts
  22  |  * classes + exams + enrollments against the curriculum
  23  |  * (`getCurriculumHistoryCounts`), and refuses with a 409 (code 5010,
  24  |  * "Archive it instead") the moment that total is above zero, EVER - there is
  25  |  * no filter for "only active" classes, so even a class that was itself
  26  |  * later deleted still counts if it briefly existed... except a genuine hard
  27  |  * delete of the CLASS removes its row entirely, so a class created and then
  28  |  * properly cleaned up (as this suite's own `deleteScratchClass` always does)
  29  |  * leaves no trace to count. The curriculum this test builds is never used
  30  |  * for anything at all - no class, no exam, no enrollment - so it stays
  31  |  * eligible for real deletion the whole time.
  32  |  *
  33  |  * **This is why the test builds its OWN, uniquely-named curriculum rather
  34  |  * than reusing `AUTOMATION_CURRICULUM_NAME`.** That shared fixture has real
  35  |  * classes and enrollments against it from dozens of other tests and will
  36  |  * NEVER pass the empty check again - proving that is this test's own
  37  |  * negative half, driven safely against the read-only side of that shared
  38  |  * fixture (a refused DELETE changes nothing, so it is safe to attempt against
  39  |  * data other tests depend on).
  40  |  *
  41  |  * Steps 1-3 are driven through the real UI (the Delete control on the
  42  |  * curriculum card, gated `canManage`, distinct from the plain member's
  43  |  * Enroll/Unenroll button and from Archive) - deletion is the thing under
  44  |  * test, so it is not shortcut through the API. Setup (creating the empty
  45  |  * curriculum) and the final verification (confirming it is genuinely gone,
  46  |  * not merely archived) go through the API, which is not what is under test.
  47  |  */
  48  | 
  49  | test.use(asRole('ownerPrimary'));
  50  | 
  51  | /** The unfiltered list - `?status` anything other than `active`/`archived` returns every curriculum regardless of `archivedAt`, which is the only way to prove a genuine delete rather than a mere archive. */
  52  | async function findAnyStatus(
  53  |   request: import('@playwright/test').APIRequestContext,
  54  |   clubId: string,
  55  |   name: string,
  56  | ): Promise<string | undefined> {
  57  |   const res = await request.get(
  58  |     `${env.apiURL}/clubs/${clubId}/class-definitions?status=all`,
  59  |     { headers: { 'x-app-id': env.appId } },
  60  |   );
  61  |   const body = await res.json().catch(() => null);
  62  |   const list: Array<Record<string, unknown>> =
  63  |     body?.data?.data ?? body?.data?.classDefinitions ?? body?.data ?? [];
  64  |   const hit = Array.isArray(list) ? list.find((c) => c?.name === name) : undefined;
  65  |   return hit ? ((hit.classDefinitionId ?? hit.id) as string) : undefined;
  66  | }
  67  | 
  68  | test.describe('Owner deletes a curriculum', () => {
  69  |   test('OWP-032 - an empty curriculum can be deleted for real @OWP-032 @destructive', async ({
  70  |     page,
  71  |     clubSection,
  72  |     clubId,
  73  |     ownerApi,
  74  |   }, testInfo) => {
  75  |     testInfo.annotations.push({
  76  |       type: 'manual-scenario',
  77  |       description: 'manual-qa/role-owner-primary.md#OWP-032 (steps 1-3)',
  78  |     });
  79  | 
  80  |     const name = scratchName('owp032-empty-curriculum');
  81  |     let classDefinitionId: string | undefined;
  82  |     let deleted = false;
  83  | 
  84  |     try {
  85  |       const created = await ownerApi.post(`${env.apiURL}/clubs/${clubId}/class-definitions`, {
  86  |         headers: { 'x-app-id': env.appId },
  87  |         data: {
  88  |           name,
  89  |           description: 'OWP-032 scratch curriculum - never used, safe to genuinely delete.',
  90  |         },
  91  |       });
  92  |       expect(created.ok(), `could not create the OWP-032 scratch curriculum (${created.status()}).`).toBeTruthy();
  93  |       const createdBody = await created.json().catch(() => null);
  94  |       classDefinitionId = createdBody?.data?.classDefinitionId ?? createdBody?.data?.id;
  95  |       expect(classDefinitionId, 'curriculum created but no id came back.').toBeTruthy();
  96  | 
  97  |       await clubSection('curricula').gotoAndAwaitClubRole(clubRoute(clubId, 'curricula'));
  98  | 
  99  |       // Step 2 - a Delete control is offered, distinct from Archive.
  100 |       const deleteTrigger = curriculaLocators.deleteTrigger(page, name);
  101 |       await expect(
  102 |         deleteTrigger,
  103 |         `the "${name}" card should offer a Delete control alongside Edit and Archive.`,
  104 |       ).toBeVisible({ timeout: 15_000 });
  105 |       await deleteTrigger.click();
  106 | 
  107 |       await expect(
  108 |         curriculaLocators.confirmDeleteDialogTitle(page),
  109 |         'clicking Delete should open the "Delete curriculum?" confirmation dialog.',
> 110 |       ).toBeVisible({ timeout: 10_000 });
      |         ^ Error: clicking Delete should open the "Delete curriculum?" confirmation dialog.
  111 | 
  112 |       // Step 3 - confirm, and watch the real DELETE response.
  113 |       const deleteCall = page.waitForResponse(
  114 |         (r) =>
  115 |           new URL(r.url()).pathname.endsWith(`/class-definitions/${classDefinitionId}`) &&
  116 |           r.request().method() === 'DELETE',
  117 |         { timeout: 15_000 },
  118 |       );
  119 |       await curriculaLocators.confirmDeleteButton(page).click();
  120 |       deleted = true; // set before awaiting - same reasoning as MEM-015
  121 |       const deleteRes = await deleteCall;
  122 |       expect(
  123 |         deleteRes.ok(),
  124 |         `DELETE .../class-definitions/:id was refused (${deleteRes.status()}) for a curriculum ` +
  125 |           'that was never used - it should have been eligible.',
  126 |       ).toBeTruthy();
  127 | 
  128 |       // Confirm it is a REAL delete, not merely hidden by an active-only
  129 |       // filter: the unfiltered list should not find it at all, archived or not.
  130 |       const stillThere = await findAnyStatus(ownerApi, clubId, name);
  131 |       expect(
  132 |         stillThere,
  133 |         'the deleted curriculum should not appear in the unfiltered list at all - ' +
  134 |           'a real delete removes the row, an archive would leave it findable with status=all.',
  135 |       ).toBeFalsy();
  136 |     } finally {
  137 |       // Belt and braces: if the UI half died before the delete landed,
  138 |       // clean up through the API so an empty scratch curriculum is never
  139 |       // left behind - unlike every OTHER curriculum fixture in this suite,
  140 |       // this one is genuinely safe to remove for real.
  141 |       if (classDefinitionId && !deleted) {
  142 |         await ownerApi
  143 |           .delete(`${env.apiURL}/clubs/${clubId}/class-definitions/${classDefinitionId}`, {
  144 |             headers: { 'x-app-id': env.appId },
  145 |           })
  146 |           .catch(() => undefined);
  147 |       }
  148 |     }
  149 |   });
  150 | 
  151 |   /**
  152 |    * The negative half - attempted against the SHARED `AUTOMATION_CURRICULUM_NAME`
  153 |    * fixture, which is safe precisely because a refused DELETE changes
  154 |    * nothing. Driven at the API level: the point is the server's own refusal,
  155 |    * not a UI journey, and there is no UI reason to expect this curriculum's
  156 |    * Delete button to behave differently from any other curriculum's - the
  157 |    * server-side check is what actually matters here.
  158 |    */
  159 |   test('OWP-032 - a curriculum with real history is refused, not silently deleted @OWP-032 @destructive', async ({
  160 |     ownerApi,
  161 |     clubId,
  162 |   }, testInfo) => {
  163 |     testInfo.annotations.push({
  164 |       type: 'manual-scenario',
  165 |       description: 'manual-qa/role-owner-primary.md#OWP-032 (step 4)',
  166 |     });
  167 | 
  168 |     const classDefinitionId = await ensureAutomationCurriculum(ownerApi, clubId);
  169 | 
  170 |     const deleteRes = await ownerApi.delete(
  171 |       `${env.apiURL}/clubs/${clubId}/class-definitions/${classDefinitionId}`,
  172 |       { headers: { 'x-app-id': env.appId } },
  173 |     );
  174 |     expect(
  175 |       deleteRes.status(),
  176 |       'deleting a curriculum with real classes/exams/enrollments against it should be ' +
  177 |         `refused with a 409 ("Archive it instead"), not silently accepted. Got ${deleteRes.status()}.`,
  178 |     ).toBe(409);
  179 | 
  180 |     const body = await deleteRes.json().catch(() => null);
  181 |     expect(
  182 |       body?.errors?.code ?? body?.error?.code,
  183 |       'the refusal should carry the documented error code 5010.',
  184 |     ).toBe(5010);
  185 | 
  186 |     // Confirm nothing was actually removed - the shared fixture every other
  187 |     // write test depends on must still exist afterward.
  188 |     const stillThereRes = await ownerApi.get(
  189 |       `${env.apiURL}/clubs/${clubId}/class-definitions/${classDefinitionId}`,
  190 |       { headers: { 'x-app-id': env.appId } },
  191 |     );
  192 |     expect(
  193 |       stillThereRes.ok(),
  194 |       'the shared AUTOMATION_CURRICULUM_NAME fixture should still exist after a refused delete.',
  195 |     ).toBeTruthy();
  196 |   });
  197 | });
  198 | 
```